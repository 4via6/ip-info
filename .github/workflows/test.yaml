name: Test Runner
on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Allows manual triggering

jobs:
  test:
    runs-on: [self-hosted]  # Simplified runner label
    steps:
      - name: Simple test
        run: |
          echo "Hello from runner"
          pwd
          ls -la
